# Screenshots

Build screenshots referenced from the DevLog session files. Drop the image files here with
the exact names below.

Used by [Session 01](../Session-01_2026-05-25.md):

| File | Shows |
| ---- | ----- |
| `level00-intro.png` | Level 0 intro line ("Motor functions... online.") |
| `level01-record.png` | Level 1 — the world-space **R** record prompt at the first plate |
| `level03-drone.png` | Level 3 — a patrol drone with its detection cone (level later cut) |

> `menu.png` (the Day-1 main menu) was removed in the v1.1.2 audit pass: the image still
> showed the pre-rename "ECHO SHIFT" title, which its caption no longer matched. It lives
> in git history.
