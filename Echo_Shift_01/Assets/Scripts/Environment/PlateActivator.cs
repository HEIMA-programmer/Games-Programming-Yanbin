using UnityEngine;

namespace EchoShift
{
    /// <summary>
    /// Marker placed on bodies that can depress pressure plates (player + echo clone).
    /// Lets plates detect activators without relying on tags or layers.
    /// </summary>
    public class PlateActivator : MonoBehaviour
    {
    }
}
